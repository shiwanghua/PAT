#include<iostream>
#include<string>
#include<memory.h>
#include <iomanip>
#include <vector>

#define _for(i,a,b) for( int i=(a); i<(b); ++i)
#define __for(i,a,b) for( int i=(a); i<=(b); ++i)

using namespace std;

int main()
{
   int m,n;
   cin>>m>>n;
   int* prime=new int[n];   //  保存质数
   bool* isP=new bool[n];   //  记录是否是质数

    int num=0; //  质数的个数

    _for(i,2,n)
    {
        
    }

    return 0;
}

